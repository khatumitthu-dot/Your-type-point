YOUR TYPE — FINAL RENDER FIX

The final server starts without DATABASE_URL.
Production admin credentials come from ADMIN_USER and ADMIN_PASS environment variables.
Static media remains in photos/.
Render uses: npm install -> node server.js
Root Directory must be empty when these files are at repository root.
